# korat
Friendly stats bot for hound.fm

# Development

Install all dependencies:

```shell
poetry install
```

Update stats:

```shell
poetry run korat
```

Format code:

```shell
poetry run black ./src
```
